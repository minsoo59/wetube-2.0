mixin write(info)
    ul.item  
        li
            a#itmeTitle(href=`/strategy/${info.id}`)=info.title   
        li #{info.user}
        li #{info.views} #{info.views === 1 ? "view" : "views"}
        li #{info.createdAt}