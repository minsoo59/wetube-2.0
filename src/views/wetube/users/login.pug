extends ../base

block content
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(placeholder="username",name="username",type="text", requried)
        input(placeholder="password",name="password",type="password", requried)
        input(type="submit", value="Login")
        br
        a(href="/wetube/users/github/start") Conninue with Github &rarr;
    hr
    div 
        span Don't have an account??  
        a(href="/wetube/join") Create one now &rarr;
