extends ../base
include ../../mixins/video

block content 
    div.video-grid
        each videoList in videos
            +videoList(videoList)
        else 
            span.empty__message No videos found