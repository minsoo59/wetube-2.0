extends ../base
include ../../mixins/video

block content 
    .video-grid
        each videoList in videos
            +videoList(videoList)
        else 
            span.empty__message No videos found